import os
import torch
from torch import nn
from torchvision.datasets import CIFAR10
from torch.utils.data import DataLoader
from torchvision import transforms

from resnet import *

sep = np.load("")
pre = np.load("")
gen = np.load("")

def model(data,n_timeSeries):

    for idx in range (n_timeSeries):



    return




